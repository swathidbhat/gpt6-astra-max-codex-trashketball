import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  metadataBase: new URL('https://trashketball-severed-to-seaside.swthbht.chatgpt.site'),
  icons: { icon: '/favicon.svg' },
  title: 'Trashketball — Out of Office',
  description: 'A little paper. A perfect arc. A much-needed break. Play two immersive 3D levels, from the severed floor to a sunlit seaside villa.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
